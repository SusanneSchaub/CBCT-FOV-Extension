# mamba env install -n EnvName -f my_conda_env.yaml          # for installing from scratch
# mamba env update -n EnvName -f my_conda_env.yaml [-prune]  # for updating via yaml file
name: naf
channels:
- conda-forge
- nvidia
- nodefaults
dependencies:
- python=3.9
- pytorch::pytorch=1.11.0
- torchvision=0.12.0
- cudatoolkit=11.3
- cudatoolkit-dev=11.3
- numpy
- setproctitle
- pip
#- pip: 
# - -r file:requirements.txt

